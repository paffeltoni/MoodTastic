Presentation KeyNotes I want to hit

* When you go to register and add your avatar the code renames the avatar and puts the image file 
in the image folder along with the database. ------ EXPLAIN FURTHER ON WHY 

* The CSS for the background- show the span and keynotes code

*PHP allows you to hash the password in one line of code

On line 31 I use my ROOT var in the header location to direct pages... show where the 
ROOT_URL is made and why its a really neat variable to have.

The responsive works on all the pages including the admin dashboard- the buttons for the tabs also change when 
it hits tablet mode then completely goes away when you get to phone size